import React from 'react';

import('./header.scss');

export default class Header extends React.Component {
  render() {
    return (
      <header>
        <h1>Counters R Fun</h1>
      </header>
    );
  }
}
